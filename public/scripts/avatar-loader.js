const avatarElement=document.getElementById("avatar"),el=document.getElementById("avatarImage");avatarElement.appendChild(el);let file;const addImageHandler=event=>{var imgURL,event=event.target.files;null!=(file=event[0])?(imgURL=URL.createObjectURL(event[0]),el.src=imgURL,el.alt=event[0].name):(el.src="",el.alt="avatar")},formDataHandler=event=>{null!=file&&event.formData.set("image",file)};DocumentHandler.whenReady(()=>{let addImageInputElement=document.getElementById("add-image-input");addImageInputElement.addEventListener("change",addImageHandler,!0);const formElement=document.getElementsByTagName("form")[0];formElement.addEventListener("formdata",formDataHandler)});